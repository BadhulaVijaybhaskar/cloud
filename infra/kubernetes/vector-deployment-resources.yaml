apiVersion: apps/v1
kind: Deployment
metadata:
  name: vector
  namespace: vector
spec:
  template:
    spec:
      containers:
      - name: vector
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "2"
            memory: "4Gi"