apiVersion: apps/v1
kind: Deployment
metadata:
  name: langgraph
  namespace: langgraph
spec:
  template:
    spec:
      containers:
      - name: langgraph
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "2"
            memory: "4Gi"